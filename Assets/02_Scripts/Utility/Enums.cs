public enum KEYTYPE
{
    COMBINE,
    DIVIDE,
    END,
}

public enum CHANGESIDE
{
    LEFT = -1,
    CENTER = 0,
    RIGHT = 1,
}

public enum CHANGER
{
    BACKGROUND,
    CIRCLE,
}

public enum LEN
{
    SELECT_CIRCLE = 3,
}

public enum CIRCLE
{
    OUT,
    IN,
    END,
}

public enum UIMODE
{
    MAIN,
    SELECT_ALBUM,
    SELECT_MUSIC,
    GAME,
}

public enum STAGEMUSIC
{
    FIRST = 5,
    SECOND = 5,
    THIRD = 5,
}

public enum SHOPKIND
{
    SKIN,
    SKILL,
    MOVIE,
    END,
}

public enum HIDE
{
    NONE,
    LEFT,
    RIGHT,
}

public enum THORNDIR
{
    INCIRCLE = 90,
    OUTCIRCLE = -90,
}

public enum CATEGORY
{
    SKIN,
    SKILL,
    VIDEO,
}

public enum POPUP
{
    SHOP,
    SETTING,
}

public enum ALBUM
{
    ISEDOL,
    CONTEST,
    GOMIX,
    WAKALOID,
}

public enum INCREASE
{
    INCREASE,
    DECREASE,
}

public enum SELECTPAGE
{
    ALBUM,
    MUSIC,
}

public enum WINDOW
{
    MAIN,
    ALBUM_SELECT,
    SONG_SELECT,
    GAME,
    PAUSE,
    SETTING,
}